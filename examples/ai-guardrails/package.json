{
  "name": "ai-guardrails-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Faithful port of ai-guardrails using @questi0nm4rk/hook-kit. Builds two binaries: hk (shell wrapper, default) for command gating, and hk-cc-tools for Claude Code's Edit/Write/Read tool-call channel.",
  "scripts": {
    "build": "bun run build:hk && bun run build:cc-tools",
    "build:hk": "hook-kit build src/hooks.ts --out dist/hk --adapter shell",
    "build:cc-tools": "hook-kit build src/hooks.ts --out dist/hk-cc-tools --adapter cc-tools"
  },
  "dependencies": {
    "@questi0nm4rk/hook-kit": "*"
  }
}
