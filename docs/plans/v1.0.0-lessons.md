# v1.0.0 implementation — lessons learned

**Purpose:** every phase's subagent reads this BEFORE starting. Each completed phase appends what was learned: what worked, what broke, patterns to repeat or avoid, surprises about the codebase. The goal is monotonic improvement — phase N+1 doesn't repeat phase N's mistakes.

**Discipline:** lessons are written by the reviewing human (not the subagent), AFTER tests run. Tag each entry with the phase that produced it. Keep entries short — rule + WHY + how-to-apply, one block each. If a lesson is repo-wide, also promote to `CLAUDE.md`.

---

## How to use this doc (subagent instructions)

When invoked for phase M`N`:

1. **Read this entire file before touching any task.** Lessons may invalidate task descriptions in `v1.0.0-tasks.md` — if a lesson contradicts a task's verify step, follow the lesson and note the conflict in your final report.
2. **Read `docs/plans/v1.0.0.md` and `docs/plans/v1.0.0-tasks.md`** to scope the phase.
3. **Implement only the tasks in your assigned phase.** Do not pre-implement future-phase tasks even if you see the opportunity. If you find a blocker that requires future-phase work, stop and report — don't silently expand scope.
4. **Verify every task with the verify step from the tasks doc.** If a verify step is impossible or wrong, that's a finding — report it, propose a fix, but do not silently substitute a weaker verification.
5. **Mark each task `[x]` in the tasks doc only after its verify step passes.** Honest accounting; partial tasks stay `[~]`.
6. **No drive-by refactors.** If you see something to clean up that isn't in your phase's scope, note it for a follow-up. Don't expand the phase.
7. **Test discipline:** every new public API ships with a test in the same PR. Tests live where the existing convention says they live (`tests/` for regular, `tests-isolated/` for anything using `mock.module()`).
8. **External validation only.** Don't claim "looks right." Run the test / lint / typecheck and cite the exit code.

---

## Phase M0 — Foundation

Completed 2026-05-21. 10/10 tasks done. One subagent (batch model), one /simplify pass, full test/lint/typecheck green.

### Lessons

**L1. tsconfig `include` must cover every dir you typecheck.**

- Subagent reported `bun run typecheck` exit 0, but the editor's diagnostics flagged 20+ type errors in `scripts/check-stable-exports.ts` and `scripts/check-changelog.ts`. Root cause: `tsconfig.json` `"include": ["src", "tests"]` — `scripts/` was silently skipped. Fixed by adding `"scripts"` and `"tests-isolated"` to include.
- **Why:** "typecheck passes" is meaningless if tsc doesn't see the files. The black-box review caught this because editor diagnostics use a broader include heuristic than CI tsc.
- **How to apply:** Any phase that adds a new top-level directory (`bench/`, `examples/adapter-template/`, etc.) MUST either extend the root tsconfig `include` glob OR ship its own `tsconfig.json`. Subagent prompts for M1+ should call this out explicitly. Verify with `bun tsc --noEmit --listFiles | grep <new-dir>/` to confirm tsc actually parsed the new dir.

**L2. Per-symbol `@stable @since 1.0.0` tags are CI-load-bearing — do NOT collapse.**

- `/simplify` flagged "per-symbol @stable redundant in pure-stable files; collapse to file-header" as MEDIUM. Skipped because TASK-002's verify step `grep -B2 "^export" src --include="*.ts" | grep -c "@stable"` depends on per-symbol counts. Collapsing would silently defeat the grep verify and break the CI guard.
- **Why:** TSDoc tooling reads file-header tags but a regex grep doesn't cascade them down to each `export`. Two consumers of the same JSDoc tag have different cascade rules; per-symbol satisfies both.
- **How to apply:** When tagging stability, default to per-symbol. Only use file-header tags in files where EVERY export shares the tier AND no grep-based check counts them. Future tag passes (e.g., `@since 1.1.0` for new APIs) should also be per-symbol.

**L3. Extract shared CI-script lib at script #2, not later.**

- M0 shipped two CI scripts (`check-stable-exports.ts`, `check-changelog.ts`) with identical `git()` + `commitRangeMessages()` + similar argv parsing. `/simplify` flagged the duplication; extracted to `scripts/_lib.ts` post-hoc. Cost: a refactor commit after the original 9 commits.
- **Why:** Two copies is the drift-floor. At copy #3 the divergence has already cost edit cycles per copy.
- **How to apply:** When a phase ships >1 file in a new directory (scripts/, bench/, examples/*/), the subagent prompt should explicitly require: "if two files share a helper of any kind, extract to a sibling `_lib.ts` (or similar) in the first PR." Don't ship the duplication and refactor later.

**L4. JSDoc tag content should be SHORT — one-line WHY, not paragraphs.**

- M0 subagent added many `@internal` blocks. `/simplify` flagged 5-10-line blocks where 1-2 would do. One file (`watch-tui.ts`) ended up with two consecutive headers in different comment styles. Fixed.
- **Why:** Verbose JSDoc rots faster than the code it documents and is harder to scan during reviews.
- **How to apply:** Subagent prompts for M1+ that involve tagging should say: "Tags are `@<tag>` + ≤1 sentence WHY. No multi-paragraph blocks. If you find yourself writing a paragraph, the explanation belongs in the file body or a sibling doc, not the JSDoc."

**L5. Subagent's "open questions" are LOAD-BEARING signal — resolve before next phase.**

- M0 subagent surfaced 5 ambiguities it resolved one way but flagged for confirmation:
  - CI-vs-lefthook for changelog enforcement (chose CI)
  - `src/engine/index.ts` @internal vs per-symbol (chose per-symbol)
  - `HookKitErrorCode` transitive export
  - JSDoc placement at declaration site vs barrel
  - tests/ + tests-isolated/ tagging
- Each is a small bet. Resolved silently they compound into surface drift before 1.0 lock.
- **How to apply:** Subagent prompts for M1+ should either: (a) pre-empt obvious ambiguities in the brief, OR (b) require the subagent to STOP mid-execution and ask via the team chat for any decision with shipped-surface impact. Don't let the subagent pick five paths it isn't sure about.

### Workflow notes (process, not code)

- **Black-box review caught the typecheck gap.** The editor's diagnostic stream surfaced what `bun run typecheck` missed. M1+ reviewer should always run `bun run typecheck` AND look at the editor diagnostic count for the changed files; mismatch = include-glob gap or similar config skip.
- **/simplify findings were 80% real, 20% wrong-context.** F5 (collapse @stable) was a false positive in this context (broke CI grep). Trust /simplify but verify against the spec before applying. Skipping a finding with a written reason is fine.
- **The batch model (one subagent for all 10 tasks) worked for M0** because the tasks were small and largely independent. For M1+ where tasks have real implementation behind them (DecisionObserver API, SqliteStateStore, etc.), micro-slice per the user's directive: 1-3 tasks per subagent invocation, each with its own tests + verify pass, then aggregate.
- **Subagent verify rigor:** the subagent ran every TASK-NNN verify command and quoted exit codes in the report. That's the bar. M1+ subagents must do the same; reviewer can spot-check, but the subagent owns proving each task done.

### Surprises about the codebase

- `src/escalation/` is bigger than expected (7 files, ~1000 lines of broker/listener/forwarder/TUI). The M1.4 escalation contract spec task will be larger than the roadmap estimated.
- `bun-types` package covers `process`, `Bun`, `node:fs`, `node:path` cleanly — no need for separate `@types/node` install. Future scripts should rely on `bun-types`.
- The existing test split (`tests/` + `tests-isolated/` with separate `bun test` invocations) is enforced by `package.json` `test` script. Don't regress.

---

## Phase M0.5 — Tooling hardening (port from ai-guardrails)

In flight. 1 of 8 batches done (S1a). Each batch is its own subagent invocation with full verify + lessons append.

### Batch S1a — lefthook scaffolding (completed 2026-05-21)

Two commits: `lefthook@1.13.6` devDep + `lefthook.yml` with biome-fix + commit-msg conventional. Lower-ceremony review (config-only batch; skipped /simplify per L-S1a-2 rationale below).

**L-S1a-1. Verify fixtures must respect the project's actual lint config.**

- Brief's verify fixture `var foo = 'bar';` could NOT auto-fix because biome's `noUnusedVariables` rule (already at error in our biome.jsonc) blocks `--write` from persisting any fix when other errors remain. Subagent had to switch to `export var foo = 'bar';` to satisfy the unused-var rule first. The `var → const` rewrite ALSO didn't happen because biome 2.x's `useConst` declines to rewrite `export var` (export semantics ambiguity).
- **Why:** biome's `--write` is all-or-nothing per file. Any unresolvable lint error blocks all fixes from persisting. Test fixtures that touch ANY lint rule with no auto-fix will silently fail the demo even though the formatter-fixable parts work.
- **How to apply:** For verify fixtures in future batches (S2 strict biome, S3a/b ESLint), use lint-free fixtures that ONLY touch formatter rules (whitespace, quote style, semicolons, line width). NEVER mix lint rules with the auto-fix demo. Or: do the demo on an explicitly-disabled rule.

**L-S1a-2. Lower-ceremony review IS appropriate for config-only batches.**

- S1a shipped `lefthook.yml` (16 lines), `package.json` (+2 lines), `bun.lock` updates. No TypeScript code added. Running /simplify on YAML + JSON would have been theater. Verified by reading both files directly + spot-checking hook shim install.
- **Why:** /simplify's three-agent review (reuse/quality/efficiency) is calibrated for code-with-control-flow. Pure config has no reuse / no flow / no perf surface.
- **How to apply:** S1b (gitleaks/codespell/markdownlint configs), S5 (CI yaml) — all config-only. Skip /simplify; spot-read instead. S2 (strict biome forces real code edits to fix violations), S3a/b (ESLint setup + rule violations), S4 (suppress-comment script in TS) — those DO need /simplify because real code lands.

**L-S1a-3. Subagent prompts referencing CLI flags need version-checked.**

- Brief told subagent to use `bun lefthook run commit-msg --commit-msg-file <path>`. That flag does not exist in lefthook 1.13.6 (and may never have). Correct invocation: positional `bun lefthook run commit-msg <path>`. Subagent caught it and adapted, but the brief should not have said the wrong thing.
- **Why:** I copy-pasted from a generic lefthook example without verifying against the installed version. Subagent's time partially wasted on brief reconciliation.
- **How to apply:** When briefing a subagent with CLI command-line examples, either (a) verify the exact invocation against the installed CLI version first, or (b) tell the subagent "verify the flag exists in the installed version before using; adapt if needed and report."

**L-S1a-4. `bun.lock` is gitignored — devDep version pinning relies on package.json alone.**

- Subagent noted: `.gitignore` excludes `bun.lock`, so the new `lefthook@1.13.6` pin is held only by `package.json`'s exact version (no caret). For reproducible installs across contributors / CI, this is fragile — `bun install` will resolve to the package.json constraint but transitive devDeps drift silently.
- **Why:** Bun's default has been to gitignore the lockfile; the npm/pnpm/yarn ecosystem committed to lockfile-in-repo. hook-kit inherited Bun's default.
- **How to apply:** **Surface to user as a S1b prereq decision.** Either: (a) un-gitignore `bun.lock`, commit it, get reproducible builds; or (b) accept transitive drift and pin via `package.json` exact versions only. ai-guardrails un-gitignores per the discipline-import principle. Hook-kit should probably follow but it's a policy call, not a M0.5 implementation detail.

**L-S1a-5. `lefthook install` writes `.git/hooks/prepare-commit-msg` too** (lefthook's own cleanup shim). Expected, harmless, informational only — every developer's `.git/hooks/` will have it.

### Workflow notes (S1a)

- **`wt merge main --no-squash --yes` works smoothly for multi-commit phases.** Rebased + fast-forwarded; auto-removed the worktree. Per-commit history preserved on main.
- **Hooks installed by S1a are now ACTIVE on every future commit.** Including my reviewer commits. Means: every commit message must conventional-format, every staged TS/JSX file gets biome-fixed in place. Note for upcoming S1b/S2 subagents: their commits will also hit the hooks; their messages must match `^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\(.+\))?!?:`.

### Batch S1b — gitleaks + codespell + markdownlint (completed 2026-05-22)

Three commits: gitleaks hook (no devDep — system binary), codespell hook + `.codespellrc`, markdownlint hook + `markdownlint-cli2@0.22.1` devDep + `.markdownlint.jsonc`. Lockfile updated atomically in the T05 commit (the first M0.5 batch since `bun.lock` was un-gitignored). Lower-ceremony review (config-only).

**L-S1b-1. Tool-specific allowlists invalidate generic security fixtures.**

- Brief told subagent to verify gitleaks with `aws_access_key_id = "AKIAIOSFODNN7EXAMPLE"`. That's in gitleaks 8.x's built-in allowlist (canonical AWS docs example key — gitleaks deliberately ignores it). Subagent had to switch to a real-shape OpenSSH private-key fragment in a `.txt` file to trigger the hook.
- **Why:** Security tools curate allowlists to avoid false positives on documentation examples. A "secret-looking" fixture might not actually look like a secret to the tool.
- **How to apply:** When briefing security-tool verify steps, use real-shape (but test-only) credentials, not famous example values. Prefer non-AWS shapes when possible (gitleaks has more AWS allowlist entries than others). The OpenSSH-key-in-.txt-file pattern works; document if you find a cleaner one.

**L-S1b-2. Spell-check ignore-words mix two distinct categories — document both.**

- Subagent seeded `ignore-words-list` with `unparseable` (valid English variant) and `absout` (deliberate variable name `absOut` in build/bundle.ts). Both belong, for different reasons. Header comment in `.codespellrc` calls out the rationale per entry.
- **Why:** Without rationale, future contributors can't tell "this is a legitimate English word codespell flags wrongly" from "this is a project-specific identifier we deliberately chose." Same risk as un-justified `// eslint-disable`.
- **How to apply:** Every new ignore-words entry needs a one-line WHY in the config header. If the entry is project-identifier-specific (like `absout`), document the file:line context. Future S4 (suppression-comment discipline) will codify this for code suppressions too.

**L-S1b-3. markdownlint defaults conflict with conventional doc formats — disable selectively.**

- Four rules tripped on existing repo docs:
  - **MD024** (no duplicate headings) — Keep-a-Changelog requires `### Added` / `### Fixed` per version. Fix: `{ "siblings_only": true }` — allows the cross-version reuse, still forbids same-level adjacency.
  - **MD060** (table-column-style) — stylistic. Pick a side; we kept padded for readability and disabled.
  - **MD040** (fenced code language tag) — common gotcha on prose-y blocks (shell-output examples, ASCII trees). Fix: tag with `text` or `shell`. Subagent fixed 8 instances in README/CHANGELOG/CLAUDE.md.
  - **MD041** (first line H1) — disabled per S1a brief; some docs lead with a context paragraph.
- **Why:** markdownlint's defaults are optimized for blog-style markdown; library README / changelog formats deviate.
- **How to apply:** When introducing markdownlint downstream (or in S1b-equivalent batches in other repos), audit against the project's actual docs first. The four-rule disable pattern above is a sane starting point for Keep-a-Changelog + table-heavy README.

**L-S1b-4. Subagent auto-merged the branch via lefthook-during-commit chain.**

- Subagent's 3 commits landed directly on main without a manual `wt merge` step. Likely path: subagent committed in its worktree → `wt` configured to auto-rebase-and-fast-forward on commit when the working tree is clean and tests pass (or the subagent ran `wt merge` itself before returning). Either way, the M0.5 review loop's "subagent reports done → reviewer runs `wt merge`" step was short-circuited.
- **Why:** Net positive (one less manual step) but means the reviewer can't black-box the worktree branch before merge — work is already on main when the report arrives.
- **How to apply:** For batches where the review must precede merge (S2 strict biome — real code changes; S3a/b ESLint adoption — same), the subagent prompt should explicitly say "do NOT run wt merge; leave the branch on its worktree for reviewer." For config-only batches (S1b confirmed; S5 likely) auto-merge is fine.

**L-S1b-5. Editor diagnostics can be stale right after worktree removal.**

- After cleaning up the 2 leftover worktree dirs (M0 + S1a husks), the editor flooded with "Cannot find module" diagnostics across half of `src/`. Was tsc-server reindexing; `bun run typecheck` returned exit 0 immediately.
- **How to apply:** Diagnostic floods after major worktree/branch operations are likely stale; verify with `bun run typecheck` before panicking.

### Workflow notes (S1b)

- **Lockfile diff lands cleanly when only one batch adds devDeps.** S1b's markdownlint-cli2 addition produced +194 lines in `bun.lock`. Reviewable in PR.
- **System-tool dependencies (gitleaks, codespell) document install paths in `lefthook.yml` header.** Pre-commit fails clearly if the binary isn't on PATH (lefthook reports the missing command). Contributors get a clear signal instead of a silent skip.
- **Subagent's open questions on S1b were all "confirm what I did is right" type** — no real ambiguity, just thoroughness. Good signal; lower future-batch prompt verbosity on questions like "is `false` the right value for `MD060`?" by pre-empting with explicit rationale in the brief.

### Batch S2 — strict biome adoption (completed 2026-05-22)

8 commits: enable-all-groups + 11 disable-at-config + Qwik disable + lefthook biome-pin + codespell `afterall` allow + noMagicNumbers re-enable per override + auto-fix combined --safe+--unsafe + CHANGELOG + comment nit-fix. 66 files changed +735/-274. 891 initial violations resolved across the pipeline. 525/525 tests still pass, CP-1 clean.

**L-S2-1. STOP-on-threshold discipline pays off.**

- Subagent stopped twice during S2: once at the initial 12-rules-over-threshold gate (surfaced for Q1-Q5 reviewer decisions), once at the `useBlockStatements` Q1+Q3 mutual-unsatisfiability discovery. Each STOP produced a correct reviewer decision instead of a silent compromise that would have shipped a bad config.
- **Why:** the subagent's per-rule violation manifest + impact assessment is the right level of detail for the reviewer to make tier-defining calls. Subagent doing it silently would have either over-disabled or over-churned.
- **How to apply:** keep the ≥20-in-src/ STOP rule in S3a/b briefs. Add a second STOP trigger: "if your work surfaces a brief contradiction (like Q3 vs Q1 for useBlockStatements), STOP and surface it — don't pick a path silently to resolve the contradiction."

**L-S2-2. Initial "no --unsafe" gate was over-cautious; Q3 reversal in flight worked cleanly.**

- The reviewer's original Q3 (no --unsafe) was about preventing noProcessGlobal/noNodejsModules import rewrites. Once those got disabled-at-config, the underlying concern vanished. Subagent surfaced the contradiction; reviewer authorized --unsafe; commit landed clean.
- **Why:** general-purpose "no --unsafe" was the right precaution INITIALLY (before knowing what would be enabled), but became a liability once specific cross-cutting rules were disabled.
- **How to apply:** for future "what's the auto-fix budget" decisions, condition the answer on which rules are enabled. Don't bake general rules that assume a rule set that may change during execution. Pattern: "no --unsafe IF rules X/Y/Z are enabled" rather than "no --unsafe ever."

**L-S2-3. Biome's `correctness` group includes framework-specific rules (Qwik, etc.) that fire on plain TS.**

- After T06 enabled `correctness: "error"`, `useQwikValidLexicalScope` fired 8 times in src/build/cli.ts / src/builders/command.ts / src/engine/index.ts on plain function expressions. Hook-kit is not a Qwik project; rule is irrelevant. Disable-with-reason landed as commit `c14d3c1` per the standard pattern.
- **Why:** biome's groups bundle framework-aware rules with general correctness rules. Group-shorthand `"error"` enables ALL members, including framework-specific.
- **How to apply:** when group-enabling biome rules in S3a (or future versions), expect 1-3 framework-specific rules to fire. Discover them in the first lint run post-enable; disable-at-config with reason. Document the running list in `biome.jsonc` so future biome upgrades that add more framework rules slot in cleanly.

**L-S2-4. Pre-commit biome must use the project pin (`bunx --bun biome`), not bare `biome`.**

- System `biome` at `/usr/bin/biome` (Arch package, 2.3.15) was being invoked by the lefthook pre-commit. Project pin is `@biomejs/biome@2.4.15` in devDependencies. Schema-version mismatch + unknown-rule errors (useQwikValidLexicalScope wasn't in 2.3.15) blocked the T07b commit until `bedba16` fixed the hook invocation.
- **Why:** lefthook YAML's `run: biome check` follows PATH lookup. PATH ordering varies per environment; nothing forces it to prefer the local devDep.
- **How to apply:** every lefthook command that calls a project devDep must use `bunx --bun <tool>` (or the equivalent invocation pattern that pins to the local node_modules) instead of bare `<tool>`. Applies retroactively to S1b's gitleaks/codespell/markdownlint hooks if those tools ever get added as project devDeps (currently they're system binaries; if they move, the same fix applies).

**L-S2-5. Auto-fix volume scales with rule severity changes; combined --safe+--unsafe commit is the right shape.**

- Going from `recommended: true` (~10 rules at error) to all-groups-at-error (~150 rules at error) produced 235 auto-fixable violations across 28 files. The single combined commit (`03738df`) bundled --safe (noNegationElse, useNumericSeparators, etc.) + --unsafe (useBlockStatements, noUselessUndefined, etc.) into one reviewable diff with a content-bearing commit body.
- **Why:** alternative was 7-10 tiny per-rule commits, each adding overhead with no review benefit. The mechanical nature of auto-fixes makes them reviewable as one batch.
- **How to apply:** for S3a/b (ESLint adoption), expect similar volume from the typescript-eslint preset. One combined "apply --fix auto-fixes" commit per linter is the right shape.

**L-S2-6. biome 2.x file-header disable syntax: `// biome-ignore-all lint/<group>/<rule>: <reason>`.**

- Distinct from per-line `// biome-ignore <rule>: <reason>` — the file-header form requires the `lint/` prefix AND the rule name with group prefix (`lint/style/noMagicNumbers`). Subagent shipped 14 file-header disables in the right form.
- **How to apply:** when writing S3a/b briefs that mention file-header disables, cite this exact syntax. Don't invent variants.

### Workflow notes (S2)

- **L-S1b-4 directive ("do NOT run wt merge; leave on worktree for reviewer") WORKED PERFECTLY.** Subagent finished, reported clean, did not merge. Reviewer ran /simplify (scoped to watch-tui.ts refactor), spot-checked biome-ignore reasons (all content-bearing), fixed one LOW-severity comment nit, then merged. This is the canonical pattern for real-code batches. Bake into every future S3a/S3b/S4 brief.
- **/simplify scope tightening was the right call.** Most of S2 was mechanical lint compliance. Running /simplify's 3-agent review across all 66 changed files would have been theater; scoping to the one genuinely-refactored file (watch-tui.ts with extracted constants + helpers) caught real findings. Future batches: scope /simplify to where genuine refactoring happened, not the whole diff.
- **Subagent's auto-discovered fixes (bedba16, c14d3c1) were legitimate scope expansions, not creep.** Both had content-bearing rationales in commit bodies. The lefthook biome-pin issue blocked progress; the Qwik-rule disable was forced by an unknown-rule surfaced after enabling. Acceptable expansion under the brief's "blockers can be fixed" implicit allowance. For future briefs, explicitly allow: "if a blocker surfaces that requires a small fix outside your scope, fix it with a content-bearing commit body explaining the necessity; don't ask me first if the fix is mechanical and unblocks your task."
- **Diagnostic floods after worktree manipulation continue to be stale per L-S1b-5.** Always re-run `bun run typecheck` before believing the editor.

### Batch S3a — ESLint scaffolding (completed 2026-05-22)

3 commits: eslint 10.4.0 + typescript-eslint 8.59.4 devDeps, `eslint.config.js` flat with `strictTypeChecked` + `stylisticTypeChecked` presets, lint script split into `lint:biome` + `lint:eslint`. 182 ESLint violations surfaced (medium band) for S3b to resolve.

**L-S3a-1. Subagent cwd persistence between Bash calls is treacherous in worktrees.**

- Subagent's first 3 commits accidentally landed on `main` because `cd ~/Projects/hook-kit && ...` patterns shifted persistent cwd OUT of the worktree. Recovered via `git reset --hard 55facae` on main (dropped accidental commits) + `git reset --hard 1857b0c` on worktree branch (pulled the commits in). End state correct, but the recovery was load-bearing.
- **Why:** Bash tool's cwd persists across calls. Subagent's brief said "use `cd` only into the worktree", but a single `cd ~/Projects/hook-kit && git log` for inspection silently moved subsequent commits to main.
- **How to apply:** Future subagent briefs MUST say: "Either `cd` into your worktree path ONCE at session start and use relative paths thereafter, OR always pass `-C <worktree-path>` to git and `--cwd <worktree-path>` to bun. NEVER `cd` to a different directory mid-session — Bash cwd persists." Bake into the S3b/S4 briefs explicitly.

**L-S3a-2. `parserOptions.projectService: true` is the modern typescript-eslint 8+ pattern.**

- Replaces `project: true`. Auto-discovers nearest tsconfig per file; faster; handles multi-tsconfig setups. Used here with `tsconfigRootDir: import.meta.dirname` for absolute resolution.
- **How to apply:** any future typescript-eslint config additions should use `projectService` not `project`.

**L-S3a-3. Intentional redundancy on `no-unused-vars`** — keep BOTH biome's `noUnusedVariables` AND `@typescript-eslint/no-unused-vars`. Type-aware variant catches strict superset (destructured fields, type-only imports). Biome's non-type-aware version catches drift if ESLint mis-parses. First flagger fails CI. Documented in `eslint.config.js` comment.

**L-S3a-4. `strict-type-checked` + `stylistic-type-checked` produced 182 violations across 36 files.** Three rule families dominate (78%): `no-unsafe-*` (67), template/void expression discipline (45), async-correctness (32). Medium-band per S3b sizing — tractable in one batch with rule-family triage.

**L-S3a-5. typescript-eslint presets are 100% error-severity out of the box** (verified by grep against the package's flat config files). Zero `warn` defaults. CP-1 holds without per-rule overrides.

### Workflow notes (S3a)

- **Skipped /simplify (config-only batch per L-S1a-2).** Spot-read `eslint.config.js` directly for CP-1 compliance + design clarity. Sufficient signal.
- **biome+ESLint dual run is straightforward.** Sequential (`bun run lint:biome && bun run lint:eslint`); short-circuit on first failure; 89-file biome + 89-file ESLint takes ~1.2s combined cold. Acceptable for `lint` script.
- **Biome owns formatting; ESLint owns type-aware logic.** Documented in `eslint.config.js` so future "add ESLint rule X" PRs check biome first. Two formatters fighting is the known anti-pattern; this guard prevents it.

### Batch S3b — strict ESLint rule resolution (completed 2026-05-22)

8 commits: T12 async-correctness overrides + T13 auto-fix (26 sites) + 5 per-family manual passes (no-floating-promises 22, require-await 10, no-unsafe-* 67, restrict-template-expressions 32, final-pass 24) + T14 CHANGELOG. 37 files changed, +337/-124. 182 ESLint violations → 0. 525/525 tests still pass, CP-1 clean across all 3 configs.

**L-S3b-1. Subagent found 4 latent test bugs while fixing `require-await`.**
- `expect(promise).rejects.toThrow(...)` without `await` silently passes in bun-test (the matcher's TS type is `void` even though runtime is async). Tests claiming to verify rejection behavior were succeeding regardless of whether the rejection matched. Subagent rewrote 4 to `try { await p } catch(e) { ... }`.
- **Why:** type system can't catch async-unawaited assertions when the matcher's declared type is sync. The lint rule that surfaced it (`@typescript-eslint/require-await`) catches the missing await indirectly via "async without await."
- **How to apply:** when ESLint's async rules fire on test files, the fix may surface latent test bugs. Don't just slap `eslint-disable` — investigate whether the test was actually testing what it claimed. Audit similar patterns in any future batch that touches `expect(...).rejects` chains.

**L-S3b-2. Subagent re-implemented S3a's package.json script split, causing a rebase conflict.**
- S3a shipped `lint:biome`/`lint:eslint`/`lint:fix` split. S3b's TASK-T14 brief said "ensure CI runs both linters" — subagent interpreted that as "set up the split" rather than "verify the split exists" and committed a re-implementation that DROPPED `scripts/` from `lint:biome` and DROPPED `bunx --bun` prefix from `lint:eslint`. Wt merge surfaced the conflict; reviewer resolved to the S3a-correct version and skipped the redundant fix commit.
- **Why:** brief wording was ambiguous between "verify" and "set up." Subagent picked the more active interpretation.
- **How to apply:** when a brief touches an area a prior batch already shipped, the brief should say explicitly: "VERIFY X exists with shape Y; if it does, no change needed. If it doesn't, surface the regression and STOP." Bake into S5+ briefs.

**L-S3b-3. Async-correctness disables go in two flavors with distinct reasons.**
- For `no-floating-promises`: either `await` the promise (preferred) OR `void promise.catch(...)` for explicit fire-and-forget. Per-line disable with reason "fire-and-forget cleanup; failure logged via .catch" is fine when fire-and-forget is intentional.
- For `require-await`: `// eslint-disable-next-line ... -- async signature is part of stable API contract; future internal awaits expected` is the canonical reason for engine entry points whose Promise return shape is part of the public contract.
- **How to apply:** the canonical-reason library above is reusable. Future ESLint rule additions: build a similar library of canonical disable-reasons per rule, document in eslint.config.js header comment block, prevent ad-hoc reason drift.

**L-S3b-4. `no-unsafe-*` rules are the leverage point for proper test fixture typing.**
- 67 violations resolved primarily by defining narrow shape interfaces (`CcStdoutJson`, `SessionMetaJson`, `DecidedJson`, `ForwardedPendingJson`) shared in test helpers + casting at the parse boundary. Better than `as any` or per-line disables — the types are real and reusable.
- **How to apply:** when the next batch surfaces a flood of `no-unsafe-*` violations, prefer defining narrow types at parse boundaries (JSON.parse, dynamic.access) over disabling. The investment compounds as more tests reuse the same shape.

**L-S3b-5. Mixed-tool disable conflicts: pick the type-narrowing tool's lint over the other's.**
- ESLint's `non-nullable-type-assertion-style` auto-fix turned `as Extract<…, {kind:"error"}>` into `errors[0]!`, which then triggered biome's `noNonNullAssertion`. Subagent reverted to the Extract pattern + disabled the eslint rule on that line; the Extract pattern is genuinely more type-narrowing (preserves the discriminated-union narrowing). Right call.
- **How to apply:** when biome and ESLint disagree on a fix, the variant that preserves more type information wins. Document the per-site decision; the lint stack's job is to surface intent, not to homogenize style.

### Workflow notes (S3b)

- **Real-code batch with /simplify-equivalent rigor:** reviewer ran regression myself in worktree, audited eslint-disable reason quality (0 lazy reasons across the diff), spot-checked the JSON-parse type narrowing, fixed the package.json regression inline.
- **Rebase-conflict-and-skip flow worked.** Subagent's redundant fix commit was skipped during `git rebase --continue`; main got the right state via fast-forward of the rebased branch.
- **Diagnostic floods STILL stale after worktree manipulation (L-S1b-5 confirmed again).** Editor showed hundreds of "Cannot find module" errors throughout the merge; `bun run typecheck` was exit 0 throughout.

### Batch S4 — suppression-comment discipline (completed 2026-05-22)

2 commits: `scripts/check-suppress-comments.ts` (534 lines) + lefthook + CI wiring. All 85 existing suppressions on main conformed under the new gate — zero pre-existing violations needed fixing. Length-check (≥10 non-whitespace chars) + denylist (TODO/see comment/needed/fix later/wip/temp/temporary/xxx) reject lazy reasons.

**L-S4-1. `bunx --bun <local-script-path>` does NOT work — L-S2-4 needs tightening.**
- L-S2-4 said "use `bunx --bun <tool>` in lefthook to pin to project devDep." That works for BINARIES from devDeps (biome, eslint, markdownlint-cli2). It does NOT work for local TS scripts at relative paths — bunx tries to resolve the path as a published package name and 404s from GitHub's tarball API.
- **Tightened rule:** lefthook commands that call project devDep BINARIES use `bunx --bun <bin>`. Lefthook commands that call local scripts use bare `bun scripts/<path>.ts`. The check-suppress-comments lefthook entry uses the latter form.
- **How to apply:** when adding new lefthook entries in S5+, check whether the invocation is a devDep binary or a local script. Don't blindly bunx everything.

**L-S4-2. Bun's strict TypeScript needs explicit `encoding: "utf8"` on `readdirSync` with `withFileTypes: true`.**
- Without it, TS infers `Dirent<NonSharedBuffer>[]` and downstream `.startsWith` / `string` ops fail because `name` is `Buffer` not `string`. Adding `encoding: "utf8"` flips the inference to string types.
- **How to apply:** any future filesystem-walk script must pass `encoding: "utf8"` to `readdirSync({ withFileTypes: true, encoding: "utf8" })`. Easy to miss; subagent caught it during T15 implementation.

**L-S4-3. Biome's `noTsIgnore` + suppressions parser scan inside JSDoc block comments.**
- The check-suppress-comments script's own docstring couldn't include literal `// @ts-ignore` or `// biome-ignore` tokens — biome's lint pass would flag them as actual suppressions on the script's own code. Subagent paraphrased the docstring to avoid the tokens.
- **How to apply:** when writing docs/JSDoc that reference suppression syntax, use paraphrased descriptions ("biome-ignore marker" not the literal). Or use backtick-quoted code spans in markdown.

**L-S4-4. Check-script structure pattern is now established for 1.0+ stability gates.**
- Four scripts in `scripts/`: `check-stable-exports.ts`, `check-changelog.ts`, `check-suppress-comments.ts`, plus shared `_lib.ts`. Common shape: argv parsing (positional file list + `--all` mode + `--help`), exit codes (0/1/2 = pass/violation/plumbing-error), stderr-write for diagnostics, lefthook + CI dual wiring.
- **How to apply:** any future stability gates (CODEOWNERS coverage, dependency-license check, etc.) should mirror this structure. Pattern is reusable.

### Workflow notes (S4)

- **`--all` mode is the right CI shape**, not `{staged_files}`. CI has no staged set; running against everything catches drift since the gate landed.
- **Pre-existing-baseline check was clean** — S2's 66 biome-ignores + S3b's 19 eslint-disables all conformed under the new gate. CP-1 + L-S1b-2 discipline from prior batches paid off: gate could land without retroactive fix-up.
- **Denylist of generic phrases is conservative-by-default.** Expanding (placeholder/tbd/n/a/???) is a one-line addition when a future reason proves too generic; subagent kept the brief's list verbatim.

### Batch S5 — CI hardening (completed 2026-05-22)

5 commits: semgrep job + coverage floor (custom script + bunfig) + markdownlint CI job + 76-violation doc fix + CHANGELOG. Three rebase-conflict resolutions during merge (test.yml + CHANGELOG.md).

**L-S5-1. Bun's native `[test] coverageThreshold` is broken in 1.3.x (3 upstream issues).**
- Confirmed empirically: with `function = 0.99` set against actual 84.65%, `bun test --coverage` exits 0. `--coverage-threshold=NN` CLI flag never existed (not in `bun test --help`). ai-guardrails' workflow has been silently passing for nothing. Tracked: oven-sh/bun#7367, #8111, #17664.
- **Workaround in hook-kit:** `scripts/check-coverage.ts` parses the text reporter's `All files` row, exits 1 below floor. Real enforcement verified by temporarily bumping the threshold + observing exit 1.
- **How to apply:** when Bun fixes upstream, flip the workaround to use the native key in `bunfig.toml` and delete the script. Until then, never trust `--coverage-threshold` on Bun.

**L-S5-2. Lefthook's local markdownlint scope was too narrow; CI's broader scope caught 76 latent violations.**
- Lefthook excludes `docs/plans/`, `docs/specs/` (those dirs are author-heavy and noisy). CI's broader `**/*.md` scope catches drift those exclusions hide. 45 of 76 violations were in `docs/plans/v1.0.0-lessons.md` (my own formatting drift across batches — exactly what the CI gate catches).
- **How to apply:** CI broader than lefthook is the right shape (local = fast/staged, CI = exhaustive). Future doc additions need to conform to MD032/MD040/MD031/MD038/MD056 (blanks around lists/fences, language tags on fences, no space in code spans, table cell-count). If lefthook scope is later widened to match CI, contributors catch drift before push instead of after.

**L-S5-3. Multi-H1 doc structure (one H1 per top-level section) requires MD025+MD001 disabled at config.**
- `docs/plans/v1.0.0-tasks.md` has `# M0 — Foundation`, `# M1 — Extension points`, etc. (one H1 per phase). Mass-demoting to `##` would cascade through every `## Mn.X` and `### TASK-NNN` heading — structural drift risk + lost semantic clarity. Disabled MD025 (multiple H1) and MD001 (heading-increment) at config with one-line WHY per CP-1 spirit.
- **How to apply:** if future docs adopt the same multi-H1 partitioning, the disables already cover them. If lessons doc drift creates similar structure, fine.

**L-S5-4. Subagent STOPs on the right things when surfaced as ≥20-violation gates.**
- S5 subagent stopped twice (T18 + T19) — once because coverage was below the 85% target and Bun's threshold key didn't work, once because 76 markdownlint violations existed pre-gate. Both STOPs surfaced real reviewer decisions instead of silent compromises. Confirms the L-S2-1 pattern is reusable across batches.
- **How to apply:** keep the ≥20-violation STOP rule in S3a/b briefs (already in place) and any future strictness-tightening batches.

### Workflow notes (S5)

- **Rebase conflict resolution mid-merge worked.** Two conflicts (test.yml CI step ordering, CHANGELOG.md entry ordering); both took-both-sides resolutions; clean fast-forward after. Pattern is reusable when sibling batches both edit shared yaml/markdown files.
- **bunfig.toml lands at repo root** alongside biome.jsonc / lefthook.yml / .markdownlint.jsonc / .codespellrc / eslint.config.js / .gitignore. The repo-root config sprawl is the cost of having a strict modern stack; acceptable, but a future "config audit" pass might consolidate where possible.
- **Multi-tool conflict on shared yaml** (test.yml grew steps across S4 + S5) is expected when ordering matters. The brief's "EXTEND, don't replace" discipline from L-S3b-2 paid off — both batches added their own step + the merge conflict was just "where in the sequence" not "what to do."

---

## Phase M0.5 — final state (S6 — completed 2026-05-22)

M0.5 complete. 8 batches shipped across 2026-05-21 → 2026-05-22. 25+ commits on main. Hook-kit's dev tooling is now ported from ai-guardrails' "super strict Python ruff ALL" discipline to the TS-stack equivalent: max-strict tsc + biome all-7-groups-at-error + typescript-eslint strict-type-checked + suppress-comment discipline + semgrep + coverage floor + markdownlint CI + 4 pre-commit hooks.

### Final M0.5 state inventory

| Tier | Tool | Config | CI gate | Pre-commit gate |
|---|---|---|---|---|
| Lint | biome 2.4.15 | `biome.jsonc` — all 6 active groups at error, 11 rules disabled-at-config with WHY | ✓ (lint job) | ✓ (`biome-fix`) |
| Lint | typescript-eslint 8.59.4 | `eslint.config.js` — strict-type-checked + stylistic-type-checked + 6 async overrides | ✓ (lint job) | ✗ (deferred — S4 covers suppression discipline) |
| Lint | markdownlint-cli2 0.22.1 | `.markdownlint.jsonc` — default + MD024 siblings_only + MD060/MD041/MD033/MD013/MD025/MD001 off-with-reason | ✓ (markdown job, full `**/*.md` scope) | ✓ (`markdownlint`, narrow scope) |
| Type | tsc 6.x | `tsconfig.json` — strict + exactOptionalPropertyTypes + noUncheckedIndexedAccess + noImplicitOverride + verbatimModuleSyntax | ✓ (lint job → bun run lint runs both linters first; typecheck job exists separately if needed) | ✗ (subagent-discovered drift caught at CI) |
| Security | semgrep | `--config auto --error` (registry default) | ✓ (semgrep job) | ✗ (Python tool, heavy for pre-commit) |
| Security | gitleaks | default ruleset | ✗ (pre-commit only — every push goes through; CI would re-scan committed history with no gain) | ✓ (`gitleaks protect --staged`) |
| Spell | codespell | `.codespellrc` — ignore-words: unparseable, absout, afterall | ✗ | ✓ (`codespell`) |
| Coverage | bun test + `scripts/check-coverage.ts` | `bunfig.toml` enables coverage; floor 84% funcs / 89% lines | ✓ (test job runs the script) | ✗ |
| Commits | lefthook commit-msg | conventional-commits regex | n/a | ✓ (`conventional`) |
| Discipline | `scripts/check-suppress-comments.ts` | ≥10-char content-bearing reason + denylist | ✓ (suppress-comments step, `--all` scope) | ✓ (`suppress-comments`, staged-files scope) |
| Discipline | `scripts/check-stable-exports.ts` | diff `src/index.ts` exports vs base ref | ✓ (PR-only) | ✗ |
| Discipline | `scripts/check-changelog.ts` | src/-touched commits require [Unreleased] edit | ✓ (PR-only) | ✗ |

### CP-1 enforced

`grep -inE '"(warn|warning|info)"' biome.jsonc eslint.config.js lefthook.yml` returns zero matches on main. Every rule across the stack is `"error"` or `"off-with-reason"` — never `"warn"`. The 525-test suite passes; biome+eslint+typecheck all exit 0.

### What remains for M1+ (not M0.5 scope)

- Coverage raise from 84%/89% to 85%+ (covered by extracting in-process tests for `src/wrapper/hk.ts` and direct factory tests for `src/core/event.ts`).
- Widen lefthook markdownlint scope to match CI (would catch drift pre-push instead of post-push).
- Filing an upstream ai-guardrails fix-CI PR for their silent-no-op `--coverage-threshold` (out of hook-kit scope but worth noting).
- Filing oven-sh/bun coverage-threshold-enforcement fix upstream (out of hook-kit scope).
- Bun version pin (`latest` is moving target; consider explicit pin in a future batch).

---

## Phase M1 — Extension points

### Batch M1.1 — DecisionObserver API (completed 2026-05-22)

7 commits: 5 implementation (types + engine wiring + per-rule timing + sha256 hash + throw-safety + zero-overhead gate + SPEC) + 2 post-implementation /simplify (collapse notify sites + add `mockObserver` testing SDK). 11/11 tasks done (TASK-012..022). 558 tests pass (525 → 558, +33 net: +31 observer + 7 mockObserver - 1 tautology dropped). Coverage rose 84.94/89.52 → **85.72/90.06** crossing the 85%/90% follow-up target from M0.5.

**L-M1.1-1. The "noop-then-overwrite closure" pattern as the zero-overhead gate.**

- Original implementation guarded every observer-firing call site with `if (observersActive) { ... }`. /simplify pass collapsed the 5 sites into a `notifyFor(args)` closure inside `evaluateInternal`, where `notifyFor` is INITIALIZED as a noop arrow and OVERWRITTEN inside an `if (observersActive)` block with the real closure. Call sites become unconditional one-liners; the work is still gated by the noop body. Same pattern applied to `notifyEngineError`, `getToolInputHash`, and the `ruleId` template-literal allocation.
- **Why:** the 5-way repetition was the single biggest readability cliff in the engine refactor. The noop-pattern lets the call sites read like "notify; the gate is the closure's problem." The cost is a small per-call args-object allocation when observersActive=false, which is JIT-elidable for this shape (escape-analyzable, immediate-died-locally object) and bounded by N rules per evaluation.
- **How to apply:** for future M1+ engine plumbing where a hot-path emission has multiple call sites all conditionally gated on the same predicate (observer firing, trace logging, metrics counter, etc.), prefer the noop-then-overwrite pattern over `if (gate) { ... }` at each site. The closure body carries the gate; the call sites stay readable.

**L-M1.1-2. `mockObserver()` in `src/testing/` is the right home for new test SDK extensions; don't expand `tests/_helpers.ts`.**

- The 5 observer test files hand-rolled `captureObserver()` closures + `event` literals + `moduleWith()` factories. /simplify Agent 1 flagged the right fix: add `mockObserver()` to `src/testing/` (matching `mockState` + `mockAskpass` precedent) as a STABLE export — downstream consumers writing their own observer integrations face the same fixture pain. `tests/_helpers.ts` gets the `moduleWith()` helper (test-internal, not stable consumer API).
- **Why:** `src/testing/` is the documented "consumers no longer hand-roll" landing pad per CLAUDE.md § 0.7.0 (test-builders SDK). New 1.0 STABLE APIs ship with their own SDK helper if the API is testable in isolation. The mockObserver design with `throwOn(record) => boolean` predicate gives consumers a clean way to exercise the observer-throw fail-open path without hand-writing a throwing observer.
- **How to apply:** every new 1.0 STABLE consumer-facing API (DecisionObserver here; ProtocolAdapter test harness in M1.2; askpass listener in M1.4; StateStore concurrency-test helpers in M1.5; SqliteStateStore in M2.1) should ship with a corresponding `mock*()` or test-builder in `src/testing/`. Pattern: a 30-50 line module with a STABLE interface, `mock()` factory, optional escape-hatch predicate, and 100% self-coverage in `tests/testing/`. Brief into every future M1+ batch.

**L-M1.1-3. STOP triggers worked in the brief; the subagent surfaced 5 calls for reviewer confirmation.**

- Brief explicitly enumerated STOP triggers (ObserverError subclass, observer-fires-per-rule-terminal-even-when-dropped, timing for engine-emitted errors, ≥20 lint violations, brief contradictions). Subagent acted on each with a defensible call and surfaced them in the final report. All 5 were ratified post-hoc.
- **Why:** the M0 L5 "subagent's open questions are load-bearing" lesson generalizes — when the brief enumerates STOP triggers explicitly, the subagent treats them as inflection points rather than silent path choices. The result is a queryable audit trail rather than 5 invisible design decisions baked into commits.
- **How to apply:** every M1+ brief should include 3-7 explicit STOP triggers covering the genuine ambiguities (not "anything weird"). The pattern that works: "if X happens, pick Y, but surface it for reviewer confirmation." The subagent picks the path AND flags it, so the orchestrator can ratify or override without re-deriving the question.

**L-M1.1-4. Subagent pushed the worktree branch to remote despite "do not push" directive in the brief.**

- Brief said "Do NOT run `wt merge`. Do NOT push to remote." Subagent committed correctly on the worktree branch but ALSO pushed `feat+m1.1-observer` to `origin/feat+m1.1-observer` at some point during implementation. Code was correct; the push was procedural drift. No reviewer harm because no PR opened.
- **Why:** likely interpretation drift on "do not push" — subagent may have interpreted as "do not push to main" / "do not open a PR" rather than "no `git push` at all." Or muscle-memory from a different workflow.
- **How to apply:** future M1+ briefs should bake `git push` blockage MORE explicitly: "Do not run `git push` for any reason. The branch must stay local-only. The reviewer pushes after merge." Also: consider checking `git ls-remote origin <branch>` post-subagent-return as a verification step; surface the violation directly in the merge step rather than recording it silently in lessons.

**L-M1.1-5. biome's `useMaxParams` caps closure args at 4 — options-object form is the workaround AND the better shape.**

- Initial `/simplify` fix design proposed `notifyFor(ruleId, ruleKind, decision, reason, label, timingMs)` — 6 positional args. biome's `useMaxParams: 4` rule (set to error per CP-1) caught it; subagent converted to `notifyFor(args: NotifyForArgs)` options-object form, matching the existing `buildRecord(args: BuildRecordArgs)` style. The options-object form ended up cleaner — call sites read as named fields rather than positional, and adding a 7th field later doesn't ripple through every call site.
- **Why:** the 4-param cap forced the right shape. The constraint compounds existing project conventions (`buildRecord` already uses options-object) and prevents future "just one more arg" drift.
- **How to apply:** any engine helper or builder with ≥3 conceptually-distinct args should default to options-object form. The biome rule catches the slip when it happens; the convention prevents it in the first place. Bake into M1.2+ briefs as a default for any new helper.

**L-M1.1-6. /simplify's 3-agent convergent findings are the high-signal subset; divergent findings are reviewer judgment calls.**

- /simplify run produced ~22 findings across the 3 agents. Convergent findings (≥2 agents flagging the same issue): collapse 5 notify sites (Agent 1 #2 MEDIUM + Agent 2 Q1 HIGH), dedupe test fixtures (Agent 1 #1 HIGH + Agent 2 Q10 LOW). Both led to high-leverage fixes.
- Divergent findings: Agent 2 said "use `.entries()` for the rule loop" (Q8 LOW); Agent 3 said "keep C-style index loop, `.entries()` is slower" (F5). I sided with Agent 3 on perf grounds; kept the loop, trimmed only the verbose unreachable-branch comments. Reviewer's job to arbitrate.
- **Why:** /simplify is a 3-agent fan-out; the 3 agents see the same diff but optimize for different axes. Convergent = the diff has a real issue; divergent = the issue is real but the right fix is contested. The orchestrator's job at /simplify-fix time is to (a) apply convergent findings reflexively, (b) make a reasoned call on divergent ones with the trade-off captured in the commit message.
- **How to apply:** when aggregating /simplify findings, sort by convergence first. The 3-agent convergence signal is more reliable than any single agent's severity rating. Cap fix scope at convergent-HIGH + convergent-MEDIUM + LOW-only-if-lessons-doc-mandates-it (e.g., L4 JSDoc brevity); skip divergent-LOW unless the trade-off is obvious.

**L-M1.1-7. Editor diagnostic floods after worktree manipulation are STALE — third confirmation.**

- L-S1b-5 (M0.5 batch S1b) first noted this. L-S3b-5 (S3b) re-confirmed. L-S5 (S5) noted it for rebase-conflict resolution. This batch: a flood right after the subagent's first session AND a flood right after `wt merge`. Both stale; `bun run typecheck` exit 0 in both cases.
- **Why:** tsc-server reindex lag after worktree-add / worktree-remove / branch-switch / merge. The editor's diagnostic stream is racing with file-system events; the CLI typecheck reads the fresh state.
- **How to apply:** for any M1+ batch involving worktree operations or merges, do NOT react to a diagnostic flood by reverting changes or assuming a regression. Run `bun run typecheck` once; if exit 0, the flood is stale. Wait 10-30s; if the editor still flags errors, restart the TS server. NEVER edit code to "fix" stale-flood errors. Note: this lesson is now a confirmed cross-phase pattern — consider promoting to CP-2.

### Workflow notes (M1.1)

- **Full /simplify pass workflow worked end-to-end.** Implementer subagent → 3-agent /simplify in parallel → orchestrator aggregates → fix list sent back to implementer via SendMessage (live agentId) → second pass commits → orchestrator manual review + merge. ~3h total from "begin M1.1" to "merged on main." This is the canonical M1+ batch shape; bake into M1.2 / M1.3 / M1.4 / M1.5 briefs.
- **`wt merge main --no-squash --yes` worked cleanly.** No conflicts. The 7-commit history (5 implementation + 2 /simplify) is preserved on main per the M0.5 convention. Worktree auto-removed post-merge.
- **`SendMessage` to a returned subagent via agentId is the right pattern for /simplify-fix.** The subagent retains full context from the implementation session, so the fix-list message can reference "the 5 notify sites" without re-explaining. Cuts the second-pass briefing from ~5KB to ~3KB.
- **L-S2's STOP-on-threshold rule (≥20-violation gates) didn't trigger** because no new lint violations surfaced. The strict biome + ESLint + suppress-comment stack from M0.5 absorbed the new code cleanly. The pattern is "the strict M0.5 stack is the floor, not a ceiling" — new M1+ code that conforms doesn't trigger STOPs because the rules pre-existed the code.

### Batch M1.2 — docs/ADAPTERS.md (completed 2026-05-22)

3 commits: subagent's bundled-docs commit (TASK-023..027) + tasks-doc marker commit + orchestrator's CLAUDE.md "8→9 HookKitError" nitpick fix. 5/5 tasks done. New 242-line `docs/ADAPTERS.md` covering the ProtocolAdapter contract, output convention (reproduced verbatim from CLAUDE.md per the dual-source-of-truth design), error-handling boundary discussion, 7 anti-patterns. Cross-references added to `src/adapters/claude-code.ts` and `src/adapters/raw.ts`.

**L-M1.2-1. Contract docs MUST quote the source signature, not paraphrase.**

- Brief specified `handleError(err, event)` for TASK-025; actual `src/adapters/types.ts` is `handleError(error: unknown)` — single arg. Subagent caught the drift while writing the doc, documented the source signature correctly, and surfaced the brief error.
- **Why:** writing a contract doc forces reading the actual interface. Briefs written from memory drift from source over time. Source files are the canonical reference; the brief is a planning aid.
- **How to apply:** future contract-doc briefs (M1.4 escalation envelope, M1.5 StateStore contract, M2.1 SqliteStateStore options) MUST instruct the subagent: "grep the interface declaration in `src/` first; quote it verbatim into the doc; write the prose against that quote, not against the brief." Bake into every contract-spec brief.

**L-M1.2-2. Cross-doc count drift is a recurring trap; codify the audit.**

- CLAUDE.md said "8 `HookKitError` subclasses" (pre-M1.1 wording). `src/core/errors.ts` has 9 classes since M1.1's `ObserverError`. New `docs/ADAPTERS.md` correctly enumerated 9. The orchestrator fixed CLAUDE.md inline as an M1.2 merge-time nitpick.
- **Why:** every doc that cites a count of something (error subclasses, builders, adapter types, stability tiers) drifts the moment that count changes. The only durable fix is a script that diffs the doc claims against the source.
- **How to apply:** file a follow-up `scripts/check-doc-counts.ts` (or similar) before M2 that diffs:
  - HookKitError subclass count (CLAUDE.md, ADAPTERS.md, SPEC.md) vs `grep -c "^export class.*HookKitError$" src/core/errors.ts`
  - Builder primitive list (CLAUDE.md table, SPEC.md) vs `ls src/builders/*.ts`
  - Adapter types (ADAPTERS.md, README adapter coverage matrix) vs `ls src/adapters/*.ts`
  Lightweight gate, prevents the next drift before it lands. Same shape as M0.5's `scripts/check-stable-exports.ts` + `scripts/check-changelog.ts`.

**L-M1.2-3. The output-convention table is now duplicated between CLAUDE.md and docs/ADAPTERS.md.**

- TASK-024 explicitly required reproducing the CLAUDE.md table verbatim. Subagent verified via `diff <(awk ... CLAUDE.md) <(awk ... docs/ADAPTERS.md)` returning empty on table rows. But the duplication is now an ongoing drift risk.
- **Why:** two sources of truth = guaranteed drift. The contract is now defined in two files; editing one without the other breaks them.
- **How to apply:** the v1.0.0.md TASK-024 already anticipated this ("future CI diff check"). File `scripts/check-output-table.ts` as a TASK-NNN before M1.5 closes. The script: extract the table rows from both files, byte-diff, exit non-zero on mismatch. ≤30 lines. Wire into CI + lefthook. The structural shape from M0.5's check-suppress-comments.ts is reusable.

**L-M1.2-4. Pure docs batches CAN be lower-ceremony but the verify gates still matter.**

- L-S1a-2 said "lower-ceremony review IS appropriate for config-only batches" — skipping /simplify. M1.2 extended this to docs batches (markdown only + 2-line source cross-refs). Skipped /simplify; spot-read the resulting `docs/ADAPTERS.md` cover-to-cover. Sufficient signal.
- But the verify gates STILL ran in full: typecheck, lint, markdownlint, coverage, suppress-comments, changelog, stable-exports. All exit 0 in the brief's report; orchestrator re-verified the markdownlint pass post-spot-read.
- **Why:** docs aren't unreviewed (Agent 2's quality dimension applies to prose: tone consistency, length, structural duplication with sibling docs). They're reviewed differently — by reading, not by 3-agent /simplify.
- **How to apply:** future docs batches (M1.4 ESCALATION.md, M1.5 STATE.md, M2's docs/BENCH.md, M5's MIGRATION-v1.0.0.md) skip /simplify; reviewer reads the doc end-to-end; verify gates run in full. /simplify stays in scope for: M1.3 adapter template (code), M1.5 TmpdirStateStore upgrade (code), M2.1 SqliteStateStore (code), M3.x builders (code), M4 testing SDK (code).

**L-M1.2-5. Single bundled commit for tightly-interlinked docs reads better than 4-5 thin commits.**

- TASK-023..027's sections cross-reference each other (§ Output convention's prose links to § Error handling's HookKitError list; § Anti-patterns cross-refs both). Subagent bundled them into one commit per the brief's guidance ("≤30 lines added per commit = bundle"). The result reads as one coherent contribution rather than five mechanical splits.
- **Why:** commit shape should match the logical chunk, not the task-decomposition shape. Five thin commits would each lack context the next one needs; one commit with a body explaining the bundle keeps the reader oriented.
- **How to apply:** M1.4 (ESCALATION.md has 4 doc tasks + the protocol_version code task + 2 worked-example tasks) is the next test. Pattern: bundle the docs (1 commit) + the code change (1 commit) + the worked example (1 commit) + the tasks-doc marker (1 commit). M1.5's StateStore contract (5 doc tasks + 1 conditional code task) similar.

### Workflow notes (M1.2)

- **"No `git push`" in the brief WORKED THIS TIME** (L-M1.1-4 fix landed). Subagent committed locally only; the pre-existing `origin/feat+m1.2-adapters` ref at base SHA was UNCHANGED. Tightening the language from "do not push to remote" to "DO NOT `git push` for any reason at all. No `git push origin <branch>`, no `git push origin main`, nothing." was the right shape — surfaced the procedural rule as a literal binding, not interpretive.
- **Mid-merge manual nitpick** (the CLAUDE.md 8→9 fix) is a legitimate M1+ pattern. The subagent surfaced the drift in its report; the orchestrator applied the one-character fix on the worktree branch BEFORE `wt merge`. Three commits landed atomically: subagent's content + subagent's marker + orchestrator's nitpick. The reviewer-applied nitpick avoids a separate cleanup commit on main.
- **Pre-existing `origin/feat+<batch>` ref pattern noted.** When `wt switch --create` creates a branch, the remote ref MAY get auto-created at the base SHA (depends on `wt` config). The subagent saw "branch is up to date with origin" and flagged it correctly — the remote ref was unchanged from before the work. Future briefs need NOT explicitly handle this (the remote ref is harmless; `git push` from the orchestrator post-merge will fast-forward main directly).
- **Markdownlint absorbed the new doc without rule changes.** S1b's `.markdownlint.jsonc` (MD024 siblings_only, MD060/MD041/MD033/MD013/MD025/MD001 disabled) covered every pattern in `docs/ADAPTERS.md`. Confirms L-S1b-3's "four-rule disable pattern is a sane starting point for Keep-a-Changelog + table-heavy README" extends to contract docs.

### Batch M1.3 — Adapter template + e2e (completed 2026-05-22)

7 commits: 5 implementation (TASK-028..033 + e2e add-on) + 2 post-/simplify cleanup. All 6 in-scope tasks done plus the new `tests/build/adapter-template-e2e.test.ts` (237→127-LOC after beforeAll hoist) validating compiled-binary wire format AND M1.1 observer wiring AND M1.2 contract conformance in one suite. Coverage rose 84.94/89.52 → **86.12/89.95** crossing the M0.5 85%+ follow-up target as a side-effect (annotation-formatter promotion to public surface pulled `src/core/annotations.ts` into the measured surface).

**L-M1.3-1. e2e tests benefit MASSIVELY from `beforeAll` hoisting of compile steps.**

- Initial e2e shape ran `stageTemplate + bun build --compile` per-test (6 cases). Wall clock: estimated ~90s. /simplify Agent 3 flagged as HIGH; the post-fix `beforeAll` hoist dropped the suite to **0.74s wall clock** — ~120× speedup. Most of the win was sharing the compiled binary; each test still spawns a fresh subprocess but the heavy work runs once.
- **Why:** `bun build --compile --bytecode` produces a 50-80MB binary that includes the bundled Bun runtime. ~10-30s per compile. Six compiles when one suffices is the primary CI cost driver for compiled-binary e2e suites.
- **How to apply:** every new `tests/build/*.test.ts` MUST hoist the staging + compile into `beforeAll` and share the binary across cases. Cleanup goes in `afterAll`. The existing `tests/build/example-ai-guardrails.test.ts` ALSO has the per-test-build pattern — Agent 3 noted as an existing wart; cleanup is its own follow-up task worth filing before M2. M1+ subagent briefs for compiled-binary e2e should bake this in by default.

**L-M1.3-2. `tsconfig paths` aliases for self-resolution leak dev/runtime divergence.**

- Initial M1.3 added `"@questi0nm4rk/hook-kit": ["./src/index.ts"]` to root `tsconfig.json` + included `examples/adapter-template` in the `include` glob. Devtime tsc resolved imports to local src; runtime (and external consumers) resolved via `node_modules/`. The paths alias would pass typecheck against API surface that doesn't exist for real consumers.
- /simplify Agent 2 flagged HIGH. Fix: drop the alias, symlink `examples/adapter-template/node_modules/@questi0nm4rk/hook-kit -> ../../../..`. The symlink mirrors the e2e staging pattern exactly; eliminates the dev/runtime divergence.
- **Why:** `tsconfig paths` is the wrong tool for self-resolution in a polyrepo / monorepo-with-published-package setup. It's fine for purely-internal monorepo packages where there's no published surface to drift from; not fine when the package IS published and external consumers consume the published shape.
- **How to apply:** any future example or test directory that needs to resolve `@questi0nm4rk/hook-kit` MUST use a symlinked `node_modules` (mirrors `tests/build/*.test.ts` staging), not a `tsconfig paths` entry. Bake into M2+ briefs.

**L-M1.3-3. Promoting `src/core/annotations.ts` formatters to public surface fixed both the drift AND raised coverage as a side-effect.**

- The template adapter's `emitOutcome` originally hand-rolled the annotation line shapes (`${label} ${kind}: ${message}` for warning/note; `${label} error: ${errorCode}: ${message}` for errors). /simplify Agent 1 #7 flagged HIGH drift: these EXACTLY duplicate `formatNonErrorAnnotation` / `formatErrorAnnotation` in `src/core/annotations.ts`, except the helpers hardcoded `"[hook-kit]"` as the fallback label while the template wanted its own `defaultLabel`.
- Fix: added optional `defaultLabel = "[hook-kit]"` parameter to both formatters; re-exported from `src/index.ts` as STABLE. Template now imports them. Closes the drift surface across runShell + CC adapter + template + future adapters.
- Side effect: coverage rose 84.95% → 86.12% funcs because the formatters were already covered by existing wrapper + CC adapter tests, but pre-promotion they weren't counted in the public-surface coverage measurement (the `--coverage` flag measures src/ uniformly; the rise is from the file becoming "more important" in the measured set, not from new tests).
- **How to apply:** when extracting a reusable formatter / utility, if the existing implementation is internal-only with a hardcoded default that a new consumer wants to override, the right fix is "add optional param + promote to public" not "hand-roll the variant in the consumer." Agent 1's "minimal version of #3" framing was exactly the right shape — closes the drift without the full `renderShellWrapperOutcome` extraction.

**L-M1.3-4. Auto-push pattern — UNRESOLVED mystery; external trigger fires ~1 minute after commits.**

- Across M1.1, M1.2 (no post-commit push observed), and M1.3 (post-commit push observed), the `feat+<batch>` branch ends up on `origin/feat+<batch>` despite explicit "no `git push`" directives in every brief. Implementer subagent confirmed via reflog inspection that it did NOT invoke `git push` at any point; commits land locally, then ~1 minute later the remote ref updates "by push" via some external mechanism.
- Investigation results so far:
  - No post-commit hook in `.git/hooks/` (only the lefthook-installed commit-msg + pre-commit + prepare-commit-msg).
  - No `core.hooksPath` override globally or locally.
  - `wt config show` exposes no autopush setting.
  - The push fires AFTER commits with ~1-minute delay, not synchronously.
  - The branch's INITIAL push happens at `wt switch --create` time (creates remote ref at base SHA).
- Likely causes (UNCONFIRMED, ordered by plausibility):
  1. A `wt`-binary background watcher / daemon I haven't identified — `wt config show` may not expose all daemons.
  2. A claude-code session-end hook from a parallel session pushing branches periodically.
  3. A user-level cron / systemd timer / git-autopush utility.
  4. An IDE / editor extension watching the repo and pushing on idle.
- **Procedural impact:** every M1+ branch ends up on remote. No code harm — the branch IS the work, just pushed earlier than the "leave for reviewer" model assumes. The reviewer still merges via `wt merge` + push of main.
- **How to apply:** stop relying on "branch stays local-only" as a discipline. Future briefs should say "do NOT open a PR" instead of "do NOT push" — PR-opening IS the reviewer-gated step that matters, push-to-remote is happening regardless. Continue investigating the trigger in M1.4 / M1.5 batches when there's a less-active background to test against. **Worth recording: this is now a 3-batch confirmed pattern.**

**L-M1.3-5. Triple-window e2e validation (M1.1 + M1.2 + M1.3) in one test suite is the right shape.**

- The new `tests/build/adapter-template-e2e.test.ts` exercises three features in 6 cases:
  - **M1.3 adapter contract** (deny/ask/clean/annotation/bad-input/observer-wired): 5 of 6 cases
  - **M1.2 output convention table** (every wire-format row): all 6 cases inherently
  - **M1.1 DecisionObserver** wiring through the binary's `run(modules, adapter, opts)` path: 1 case (OBSERVER)
- One suite, one CI run, three features validated. The triple-window framing came from the reviewer's "include some e2e tests for the other features that you are implementing" directive in the M1.3 brief.
- **Why:** each M1+ batch lands new code in a context that exercises OTHER M1+ code in production paths. The e2e suite for batch N should validate batch N PLUS its integration with batches 1..N-1, not just batch N in isolation. Single-feature e2e tests miss integration bugs.
- **How to apply:** M1.4 (ESCALATION.md + protocol_version) e2e suite should validate the escalation protocol AND M1.1 observers firing on `ask` decisions that route through the escalation tree. M1.5 (StateStore contract) should validate state-store contract AND M1.1 observers firing on `state.flush()` errors. M2.1 (SqliteStateStore) e2e should validate the new store AND every prior M1 surface that touches state. The triple-window framing scales — every new batch is N=current, validates N + (N-1) + (N-2) features in one suite.

### Workflow notes (M1.3)

- **/simplify pass workflow now battle-tested across 3 batches.** M1.1 (5 convergent findings → 9 fixes), M1.3 (8 convergent + single-agent findings → 8 fixes). Pattern: 3 parallel reviewer agents → orchestrator aggregates by convergence + applies-vs-defers triage → fix list to implementer via SendMessage (live agentId) → second-pass commits → orchestrator merge-time verification. ~3-5h end-to-end per batch.
- **Cross-doc count drift caught by deliberate brief construction**: the M1.3 brief told the subagent to "re-verify against `src/adapters/types.ts` BEFORE writing prose or code (L-M1.2-1)" and "quote source signatures verbatim." Subagent quoted `handleError(error: unknown)` correctly (1-arg, not the brief's earlier-batch `(err, event)` mis-spec). L-M1.2-1's tightening WORKED.
- **CLAUDE.md edits during a batch are now legitimate orchestrator/subagent work**: M1.2's "8→9 HookKitError" merge-time nitpick was an orchestrator pass; M1.3's testing section update was applied by the implementer per fix F. Both lifecycle-correct — CLAUDE.md is the project's living doc, not frozen.
- **e2e suite timing went from estimated ~90s to 0.74s wall clock post-/simplify.** Single biggest perf win in any /simplify pass to date. Bake the `beforeAll` hoist into every future compiled-binary e2e brief.
- **Coverage threshold MOVES UPWARD as M1 progresses.** Started at 84.94 (post-M0.5), 84.58 (post-M1.1 e2e), 84.95 (post-M1.2), now 86.12 (post-M1.3). The 85% follow-up target is met. Worth considering: raise the floor in `bunfig.toml` to 85/89 in M1.5 close-out to lock in the gains.

---

## Phase M2 — Production primitives

_To be appended after M2 completes._

---

## Phase M3 — Builder coverage

_To be appended after M3 completes._

---

## Phase M4 — Testing SDK growth

_To be appended after M4 completes._

---

## Phase M5 — Release

_To be appended after M5 completes._

---

## Cross-phase patterns

_Promoted from phase-specific lessons when the pattern recurs._

### CP-1. NO `warn` SEVERITY IN ANY STATIC-CHECK CONFIG — Binary: `error` (blocks) or `off` (explicitly disabled with reason)

**The rule.** Every lint / static-analysis tool we configure (biome, ESLint when it lands in S3a, any future scanner) MUST use `"error"` or `"off"` severity. `"warn"` / `"warning"` / `"info"` are **forbidden** in our configs. If something matters enough to flag, it matters enough to block. If it doesn't matter enough to block, disable it explicitly with a one-line WHY.

**Why.**

- Warning fatigue: warnings accumulate and get ignored. "X warnings" in CI output becomes background noise; the genuinely important new warning hides among the chronic ones.
- Forced decision: "make it pass or disable it" forces an explicit, reviewed decision per rule. There is no "I'll address it later" middle ground that silently becomes "never."
- Honest CI signal: `bun run lint` exit 0 means "every enabled rule passes." It cannot mean "passes except for the 47 chronic warnings we've stopped reading."
- Consistent with project ideology: hook-kit's own decision vocabulary uses `deny` (block) and `ask` (decide explicitly) terminals; `warning` / `note` are annotations that ALWAYS run alongside an explicit decision. The lint stack mirrors: every flagged rule produces a decision (error/off), never a "kinda flag."

**Scope.**

- ✅ Applies to: biome.jsonc rule severities, ESLint rule severities (S3a+), any future codespell-like config that has severity levels.
- ❌ Does NOT apply to: hook-kit's `warning` decision kind (that's an annotation produced by RULES at runtime for end-user agent feedback, totally separate concept). lefthook `fail_text` (already binary — hook either passes or blocks). markdownlint defaults (binary). codespell (binary). gitleaks (binary).

**Concrete applications.**

- S2 (strict biome): every rule in the 7 groups must end up `"error"` or `"off"`. Use `"off"` with a one-line WHY for rules we deliberately disable (the WHY goes in a sibling comment, JSONC-style). Never `"warn"`.
- S3a/b (ESLint): same — every rule from `strict-type-checked` + `stylistic-type-checked` + any extras lands as `error` or `off-with-reason`. If `@typescript-eslint`'s recommended preset defaults a rule to `warn`, explicitly upgrade or disable.
- Future: same applies to any new tooling. When porting from ai-guardrails or elsewhere, audit severity levels before merging the config; flip any `warn` to a deliberate `error` / `off` decision in the porting commit.

**Verify command for future audits.**

```bash
grep -inE '"(warn|warning|info)"' biome.jsonc .eslintrc.* eslint.config.* lefthook.yml 2>/dev/null
# Expect: zero matches. Any match is a rule violation.
```

**This rule was set 2026-05-22 in response to the M0.5 phase. It is durable — applies to every future batch, every future tool, every future repo policy port. Promote to the consumer-facing project README's "How we keep code quality" section if/when one is written.**
